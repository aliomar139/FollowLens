<?xml version="1.0" encoding="utf-8"?>
<!--
  A heading inside a list. The hairline runs to the end of the row so the
  grouping is visible even when the label is short, and the label sits on it
  rather than above it, which keeps the whole heading one object.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingStart="4dp"
    android:paddingTop="20dp"
    android:paddingEnd="4dp"
    android:paddingBottom="4dp">

    <TextView
        android:id="@+id/section_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="12dp"
        style="@style/Text.StatLabel" />

    <View
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:layout_weight="1"
        android:background="@color/hairline" />

</LinearLayout>
