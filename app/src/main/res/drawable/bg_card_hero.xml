<?xml version="1.0" encoding="utf-8"?>
<!--
  The one card per screen that carries the headline number. It is tinted toward
  the accent rather than sized larger, so it leads by material instead of by
  taking more room.
-->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:angle="240"
                android:startColor="@color/surface_muted"
                android:centerColor="@color/surface_raised"
                android:endColor="@color/surface" />
            <corners android:radius="24dp" />
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/accent_wash" />
            <stroke android:width="1dp" android:color="@color/edge_light" />
            <corners android:radius="24dp" />
        </shape>
    </item>
</layer-list>
